apiVersion: v1
kind: Service
metadata:
  name: hw4-web-service
  labels:
    app: hw4-web
spec:
  type: ClusterIP
  selector:
    app: hw4-web
  ports:
    - name: http
      port: 80
      targetPort: 8000