apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hw4-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hw4-web-service
                port:
                  number: 80